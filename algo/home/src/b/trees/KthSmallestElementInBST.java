package b.trees;

public class KthSmallestElementInBST {

    // There are 2 strategies to traversing a tree
    // DFS - scan from root to leaf (Pre-Order, Inorder, Postorder) depending on the order
    // BFS - we scan level by level. Nodes on higher level will be visited before ones with lower level



}
